namespace ReMarkableRemember.Services.TabletService.Models;

public enum TabletError
{
    Unknown,
    SshNotConfigured,
    SshNotConnected,
    UsbNotConnected,
    UsbNotActived,
    NotSupported,
}
